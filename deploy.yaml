---
- name: Create a small file on target server
  hosts: servers          # Inventory group name
  become: yes             # Use sudo if needed
  tasks:
    - name: Create test file
      file:
        path: /tmp/testfile.txt
        state: touch
        mode: '0644'
